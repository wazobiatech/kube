apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-anubis
  labels:
    {{- include "kube.labels" . | nindent 4 }}
    app.kubernetes.io/component: anubis
spec:
  type: {{ .Values.services.anubis.service.type }}
  ports:
    - port: {{ .Values.services.anubis.service.port }}
      nodePort: {{ .Values.services.anubis.service.nodePort }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "kube.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: anubis
